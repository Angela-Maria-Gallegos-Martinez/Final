import { VideoHomePipe } from './video-home.pipe';

describe('VideoHomePipe', () => {
  it('create an instance', () => {
    const pipe = new VideoHomePipe();
    expect(pipe).toBeTruthy();
  });
});
